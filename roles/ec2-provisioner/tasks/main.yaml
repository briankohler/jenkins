---

- include: "rds.yaml"
  when:
    - rds_database is defined
    - rds_database != ""
- include: "instance.yaml"

- include: "alb.yaml"
  when:
    - lb_protocol == "HTTP" or lb_protocol == "HTTPS"

- include: "nlb.yaml"
  when:
    - lb_protocol == "TCP"


 
